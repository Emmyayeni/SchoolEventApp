const fs = require('node:fs');
const path = require('node:path');
const root = path.resolve(__dirname, '..');
const names = ['202609130001_account_approval.sql', '202609140001_mobile_data.sql'];
const sql = '-- Generated by node scripts/prepare-database.cjs\n-- For the existing NSUK Events database, after its baseline schema.\n\n' + names.map(name =>
  `-- Migration: ${name}\n${fs.readFileSync(path.join(root, 'supabase/migrations', name), 'utf8')}`
).join('\n\n');
const output = path.join(root, 'supabase/APPLY_MOBILE_UPDATES.sql');
fs.writeFileSync(output, sql);
console.log(`Prepared ${output}`);
